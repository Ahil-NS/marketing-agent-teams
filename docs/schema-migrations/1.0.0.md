# Schema Version 1.0.0 — Initial Baseline

**Release Date:** 2026-03-01
**Type:** Initial release (no migration needed)

## Schema Fields

This is the initial SKILL.md schema version. All existing agents conform to this schema.

### Required Fields

| Field | Type | Description |
|---|---|---|
| `name` | `string` | Kebab-case agent identifier |
| `description` | `string` | Agent purpose description |
| `cluster` | `enum` | Agent cluster classification |

### Optional Fields (with defaults)

| Field | Type | Default | Description |
|---|---|---|---|
| `model` | `enum` | `haiku` | AI model tier |
| `tools` | `string[]` | `[]` | Allowed SDK tools |
| `trustTier` | `enum` | `builtin` | Trust level |
| `schemaVersion` | `string` | `1.0.0` | Schema version (semver) |
| `permissions` | `object` | `{}` | Permission boundaries |
| `examples` | `array` | — | Test inputs |

## Notes

- This is the baseline version. No migration is required.
- All 26 agents shipped with MAT conform to this schema.
- See `docs/agent-skill-schema.md` for full field documentation.
